aws.dynamodb.endpoint=http://dynamodb-local:8000
aws.region=us-east-1

# Dummy credentials required by DynamoDB Local (any non-empty value works)
aws.accessKeyId=local
aws.secretKey=local

logging.level.com.bettingsim=DEBUG
spring.main.banner-mode=off
